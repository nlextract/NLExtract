Hieronder staan scripts m.n. om je top10-extract installatie te testen.

De scripts gaan uit van de actuele Top10NL versie 1.1.1. Scripts en data
voor de oude, 1.0-versie van Top10NL staan onder de map 1.0/.

Om de scripts te draaien moet je een PostgreSQL database hebben
aangemaakt met een PostGIS template (zie PostGIS documentatie).

Pas evt het bestand ../bin/top10-settings.ini aan voor de database
connectie gegevens.

Op Linux/Mac draai je het script:

./top10-test.sh

Op Windows de .cmd scripts.

